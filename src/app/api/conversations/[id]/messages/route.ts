import { NextResponse } from "next/server";
import { getUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { const user = await getUser(); if (!user) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const { id } = await params; const account = await prisma.user.findUnique({ where: { authUserId: user.id }, select: { id: true } }); const conversation = account ? await prisma.conversation.findUnique({ where: { id }, include: { connection: true } }) : null; if (!account || !conversation || ![conversation.connection.userAId, conversation.connection.userBId].includes(account.id)) return NextResponse.json({ error: "Not found" }, { status: 404 }); const messages = await prisma.message.findMany({ where: { conversationId: id }, orderBy: { createdAt: "asc" }, take: 50, include: { sender: { select: { id: true } } } }); return NextResponse.json({ messages }); }